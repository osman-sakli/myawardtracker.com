# Backend environment — populated by `terraform output` after provisioning.
# For local testing only; in AWS these are set by Terraform on the Lambdas.
TABLE_NAME=myawardtracker-prod-data
EVIDENCE_BUCKET=myawardtracker-prod-evidence-339712706640
USER_POOL_ID=us-east-1_xxxxxxxxx
USER_POOL_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
SSM_PREFIX=/myawardtracker/prod
CORS_ORIGIN=https://myawardtracker.com
AWS_REGION=us-east-1
